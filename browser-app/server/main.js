import { Meteor } from 'meteor/meteor';

Meteor.startup(() => {
  // code to run on server at startup
});

/*
Router.route('/', {
	name: 'login',
    template: 'login'
});

Router.route('/register', {
	name: 'register',
    template: 'register'
});

/*
Router.route('/imports/ui/signup.html', function () {
  this.render('/signup');
});
*/